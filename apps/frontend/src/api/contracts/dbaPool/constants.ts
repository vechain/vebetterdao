/**
 * DBA Pool Constants
 */

/**
 * DBA eligibility threshold in percentage format (0-100)
 * Apps must have less than this percentage of votes to be eligible for DBA
 */
export const DBA_ELIGIBILITY_THRESHOLD_PERCENTAGE = 7.5

/**
 * DBA eligibility threshold in scaled format (100 = 1%)
 * Apps must have less than this scaled value to be eligible for DBA
 */
export const DBA_ELIGIBILITY_THRESHOLD_SCALED = 750
