{
  "name": "@repo/lambda",
  "version": "0.0.1",
  "main": "index.ts",
  "types": "index.ts",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "yarn prebuild && find startRound -name '*.ts' -exec sh -c 'yarn esbuild {} --bundle --minify --sourcemap --platform=node --outfile=dist/$(basename {} .lambda.ts)/index.js' \\;",
    "postbuild": "find dist/* -type d -exec sh -c 'cd {} && zip -r index.zip *' \\;",
    "test": "yarn build && jest"
  },
  "dependencies": {
    "@repo/contracts": "*",
    "dotenv": "^16.4.5",
    "lambda-local": "^2.1.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.136",
    "@vechain/sdk-core": "^1.0.0-beta.1",
    "@vechain/sdk-network": "^1.0.0-beta.1",
    "aws-lambda": "^1.0.7",
    "ethers": "^6.11.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2"
  }
}
