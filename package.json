{
  "name": "b3tr-monorepo",
  "private": true,
  "scripts": {
    "test": "turbo run test --",
    "build": "dotenv -e .env -- turbo build",
    "build:staging": "dotenv -e .env -- turbo build",
    "dev": "dotenv -e .env -- turbo dev --",
    "dev:staging": "dotenv -e .env -- turbo dev --",
    "lint": "turbo lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "contracts:compile": "dotenv -e .env -- turbo run compile --scope=contracts",
    "contracts:deploy": "dotenv -e .env -- turbo run deploy --scope=contracts",
    "contracts:test": "turbo run test --scope=contracts",
    "contracts:test:thor-solo": "dotenv -e .env -- turbo run test:thor-solo --scope=contracts",
    "solo-up": "make solo-up"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "dotenv-cli": "latest",
    "prettier": "^3.2.4",
    "turbo": "^1.11.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
