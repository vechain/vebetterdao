{
  "name": "b3tr-monorepo",
  "private": true,
  "scripts": {
    "test": "dotenv -e .env.dev -- turbo run test --",
    "build": "dotenv -e .env.dev -- turbo build",
    "build:staging": "dotenv -e .env.staging -- turbo build",
    "dev": "dotenv -e .env.dev -- turbo dev --",
    "staging": "dotenv -e .env.staging -- turbo staging --",
    "lint": "turbo lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "contracts:compile": "turbo run compile --scope=contracts",
    "contracts:deploy": "dotenv -e .env.dev turbo run deploy --scope=contracts",
    "contracts:deploy:staging": "dotenv -e .env.staging turbo run deploy --scope=contracts",
    "contracts:test": "turbo run test --scope=contracts",
    "contracts:test:thor-solo": "turbo run test:thor-solo --scope=contracts",
    "solo-up": "make solo-up"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "dotenv-cli": "latest",
    "prettier": "^3.2.4",
    "turbo": "^1.11.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
