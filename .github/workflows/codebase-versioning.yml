name: Codebase Versioning

on:
  pull_request:
    branches:
      - main
    types: [closed]

jobs:
  semantic-versioning:
    uses: vechain/github-actions/.github/workflows/semantic-versioning.yaml@62cc6d6dc08c6a49518766e877942685f080b443 # v.1.9.0
    secrets:
      DEPLOY_KEY: ${{ secrets.VECHAINCI_SSH_PRIVATE_KEYS }}
