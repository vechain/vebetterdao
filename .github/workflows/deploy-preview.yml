name: Deploy Preview Environment

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
      - dev-testnet
  workflow_dispatch:
    inputs:
      flavor:
        description: "Preview Flavor"
        required: true
        type: choice
        options:
          - staging
          - dev-testnet
          - prod
        default: "prod"

concurrency:
  group: preview-pr-${{ github.event.pull_request.number || github.ref_name }}
  cancel-in-progress: true

env:
  # AWS Configuration
  AWS_REGION: eu-west-1
  ECR_REPOSITORY: governance-frontend
  TERRAFORM_VERSION: 1.6.0

  # Project Configuration
  PROJECT_NAME: governance-frontend
  PREVIEW_DOMAIN_SUFFIX: preview-staging.governance.vebetterdao.org

  # Terraform Paths
  TERRAFORM_DIR: terraform/frontend/frontend-env-level
  TERRAFORM_ENV_DIR: terraform/frontend/environments/env
  TERRAFORM_BACKEND_CONFIG: ../environments/env/preview/backend.config

jobs:
  prepare-metadata:
    name: Prepare Metadata
    runs-on: ubuntu-latest
    outputs:
      flavor: ${{ steps.metadata.outputs.flavor }}
      domain_env: ${{ steps.metadata.outputs.domain_env }}
      pr_number: ${{ steps.metadata.outputs.pr_number }}
      short_sha: ${{ steps.metadata.outputs.short_sha }}
    steps:
      - name: Determine Flavor and Params
        id: metadata
        run: |
          # Default values
          FLAVOR=""
          DOMAIN_ENV=""
          SHORT_SHA=""
          PR_NUMBER=""
          
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            FLAVOR="${{ inputs.flavor }}"
            SHORT_SHA=$(git rev-parse --short HEAD)
            PR_NUMBER=$(gh pr list --head ${{ github.ref_name }} --state open --json number -q '.[0].number' || echo "" && exit 1)
          elif [ "${{ github.event_name }}" == "pull_request" ]; then
            SHORT_SHA=$(echo ${{ github.event.pull_request.head.sha }} | cut -c1-7)
            PR_NUMBER="${{ github.event.pull_request.number }}"

            if [ "${{ github.base_ref }}" == "dev-testnet" ]; then
              FLAVOR="dev-testnet"
            elif [ "${{ github.base_ref }}" == "main" ]; then
              FLAVOR="staging"
            else
              echo "Could not determine preview flavor from base ref"
              exit 1
            fi

          else
            echo "Unrecognised github event"
            exit 1
          fi

          # Map flavor to domain pattern
          case $FLAVOR in
            "dev-testnet")
              DOMAIN_ENV="dev.testnet"
              ;;
            "staging")
              DOMAIN_ENV="staging"
              ;;
            "prod")
              DOMAIN_ENV="beta"
              ;;
            *)
              echo "Unrecognised flavor: ${FLAVOR}"
              exit 1
              ;;
          esac
          
          echo "flavor=${FLAVOR}" >> $GITHUB_OUTPUT
          echo "domain_env=${DOMAIN_ENV}" >> $GITHUB_OUTPUT
          echo "pr_number=${PR_NUMBER}" >> $GITHUB_OUTPUT
          echo "short_sha=${SHORT_SHA}" >> $GITHUB_OUTPUT

  pr-comment:
    name: Post Initial PR Comment
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    outputs:
      comment_id: ${{ steps.comment.outputs.comment_id }}

    steps:
      - name: Post initial PR comment (Building)
        id: comment
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.issue.number;
            const commitSha = context.payload.pull_request.head.sha.substring(0, 7);
            const commitUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/commit/${context.payload.pull_request.head.sha}`;
            const projectName = process.env.PROJECT_NAME;

            const body = `## ${projectName.charAt(0).toUpperCase() + projectName.slice(1).replace(/-/g, ' ')} – Preview

            | Name | Status | Preview | Updated (UTC) |
            |------|--------|---------|---------------|
            | **${projectName}-preview** | 🔨 Building ([${commitSha}](${commitUrl})) | Building image... | ${new Date().toISOString().replace('T', ' ').substring(0, 19)} |

            ---
            <sub>Building with commit ${commitSha} • Preview environments are automatically destroyed when PRs are closed</sub>`;

            // Find existing comment
            const comments = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
            });

            const existingComment = comments.data.find(comment => 
              comment.user.type === 'Bot' && 
              comment.body.includes(`${projectName.charAt(0).toUpperCase() + projectName.slice(1).replace(/-/g, ' ')} – Preview`)
            );

            let commentId;
            if (existingComment) {
              // Update existing comment
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existingComment.id,
                body: body
              });
              commentId = existingComment.id;
            } else {
              // Create new comment
              const comment = await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: body
              });
              commentId = comment.data.id;
            }

            console.log(`Comment ID: ${commentId}`);
            core.setOutput('comment_id', commentId);

  build-and-push:
    needs: [prepare-metadata]
    uses: ./.github/workflows/build-push-ecr.yml
    with:
      ecr_repository: governance-frontend
      image_tag: pr-${{ needs.prepare-metadata.outputs.pr_number }}-${{ needs.prepare-metadata.outputs.short_sha }}
      aws_region: eu-west-1
      environment: "AWS ${{ needs.prepare-metadata.outputs.flavor }} governance build-time"
    secrets:
      aws_role_arn: ${{ secrets.DEV_AWS_ACC_ROLE }}

  deploy:
    name: Deploy Preview Environment
    runs-on: ubuntu-latest
    needs: [prepare-metadata, build-and-push]
    if: success()
    permissions:
      id-token: write
      contents: read
    environment:
      name: preview-pr-${{ github.event.pull_request.number }}
      url: https://pr-${{ github.event.pull_request.number }}.preview-staging.governance.vebetterdao.org

    outputs:
      custom_url: ${{ steps.outputs.outputs.custom_url }}
      service_url: ${{ steps.outputs.outputs.service_url }}
      custom_domain_status: ${{ steps.outputs.outputs.custom_domain_status }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ secrets.DEV_AWS_ACC_ROLE }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}
          terraform_wrapper: false

      - name: Install yq
        run: |
          wget https://github.com/mikefarah/yq/releases/download/v4.44.1/yq_linux_amd64 -O /usr/local/bin/yq
          chmod +x /usr/local/bin/yq

      - name: Create preview environment config
        run: |
          # Get values from prepare-metadata job
          PR_NUMBER="${{ needs.prepare-metadata.outputs.pr_number }}"
          SHORT_SHA="${{ needs.prepare-metadata.outputs.short_sha }}"
          FLAVOR="${{ needs.prepare-metadata.outputs.flavor }}"
          DOMAIN_ENV="${{ needs.prepare-metadata.outputs.domain_env }}"
          
          # Map flavor to source config directory
          # (flavor names don't always match directory names)
          case $FLAVOR in
            "dev-testnet") SOURCE_DIR="dev" ;;
            "prod") SOURCE_DIR="beta" ;;
            *) SOURCE_DIR="${FLAVOR}" ;;
          esac
          
          # Define paths
          WORKSPACE_NAME="preview-pr-${PR_NUMBER}"
          IMAGE_TAG="pr-${PR_NUMBER}-${SHORT_SHA}"
          SOURCE_CONFIG="${{ env.TERRAFORM_ENV_DIR }}/${SOURCE_DIR}/${SOURCE_DIR}.yaml"
          TARGET_DIR="${{ env.TERRAFORM_ENV_DIR }}/${WORKSPACE_NAME}"
          TARGET_FILE="${TARGET_DIR}/${WORKSPACE_NAME}.yaml"
          
          echo "=== Preview Config Generation ==="
          echo "PR Number: ${PR_NUMBER}"
          echo "Short SHA: ${SHORT_SHA}"
          echo "Flavor: ${FLAVOR}"
          echo "Source Dir: ${SOURCE_DIR}"
          echo "Domain Env: ${DOMAIN_ENV}"
          echo "Image Tag: ${IMAGE_TAG}"
          echo "Source Config: ${SOURCE_CONFIG}"
          echo "Target File: ${TARGET_FILE}"
          echo ""
          
          # Step 1: Create workspace directory
          mkdir -p "${TARGET_DIR}"
          
          # Step 2: Copy the preview template
          cp "${{ env.TERRAFORM_ENV_DIR }}/preview/preview.yaml.example" "${TARGET_FILE}"
          
          # Step 3: Replace placeholders in the template
          sed -i "s/{PR_NUMBER}/${PR_NUMBER}/g" "${TARGET_FILE}"
          sed -i "s/{COMMIT_SHA}/${SHORT_SHA}/g" "${TARGET_FILE}"
          sed -i "s/{ENVIRONMENT}/${DOMAIN_ENV}/g" "${TARGET_FILE}"
          
          # Step 4: Append flavor-specific config from source environment
          echo "" >> "${TARGET_FILE}"
          echo "# ============================================" >> "${TARGET_FILE}"
          echo "# Dynamic Config from ${FLAVOR} flavor (${SOURCE_DIR})" >> "${TARGET_FILE}"
          echo "# ============================================" >> "${TARGET_FILE}"
          
          # Override image tag
          echo "image_tag: ${IMAGE_TAG}" >> "${TARGET_FILE}"
          
          # Extract and append SSM config from source environment
          if [ -f "${SOURCE_CONFIG}" ]; then
            echo "Extracting config from ${SOURCE_CONFIG}..."
            
            # Extract ssm_parameter_prefix, runtime_env_var_names, and runtime_env_secret_names
            # Default lists to empty arrays if missing, omit ssm_parameter_prefix if null
            yq eval '{
              "ssm_parameter_prefix": .ssm_parameter_prefix,
              "runtime_env_var_names": (.runtime_env_var_names // []),
              "runtime_env_secret_names": (.runtime_env_secret_names // [])
            } | del(.ssm_parameter_prefix | select(. == null))' "${SOURCE_CONFIG}" >> "${TARGET_FILE}"
          else
            echo "Warning: Source config ${SOURCE_CONFIG} not found. Using defaults."
            # Ensure lists are present to prevent Terraform errors
            echo "runtime_env_var_names: []" >> "${TARGET_FILE}"
            echo "runtime_env_secret_names: []" >> "${TARGET_FILE}"
          fi
          
          # Step 5: Show the generated config for debugging
          echo ""
          echo "=== Generated Preview Config ==="
          cat "${TARGET_FILE}"

      - name: Terraform Init
        working-directory: ${{ env.TERRAFORM_DIR }}
        run: terraform init -backend-config=${{ env.TERRAFORM_BACKEND_CONFIG }}

      - name: Terraform Workspace
        working-directory: ${{ env.TERRAFORM_DIR }}
        run: |
          WORKSPACE_NAME="preview-pr-${{ needs.prepare-metadata.outputs.pr_number }}"
          terraform workspace select ${WORKSPACE_NAME} || terraform workspace new ${WORKSPACE_NAME}

      - name: Terraform Plan
        working-directory: ${{ env.TERRAFORM_DIR }}
        run: |
          terraform plan -out=tfplan

      - name: Terraform Apply
        working-directory: ${{ env.TERRAFORM_DIR }}
        run: |
          terraform apply -auto-approve tfplan

      - name: Get deployment URL
        working-directory: ${{ env.TERRAFORM_DIR }}
        id: outputs
        run: |
          CUSTOM_URL=$(terraform output -raw custom_domain_url || echo "")
          SERVICE_URL=$(terraform output -raw service_url || echo "")
          DOMAIN_STATUS=$(terraform output -raw custom_domain_status || echo "unknown")
          echo "custom_url=${CUSTOM_URL}" >> $GITHUB_OUTPUT
          echo "service_url=${SERVICE_URL}" >> $GITHUB_OUTPUT
          echo "custom_domain_status=${DOMAIN_STATUS}" >> $GITHUB_OUTPUT

  update-comment:
    name: Update PR Comment
    runs-on: ubuntu-latest
    needs: [pr-comment, prepare-metadata, build-and-push, deploy]
    if: always() && needs.pr-comment.result == 'success'
    permissions:
      pull-requests: write

    steps:
      - name: Update PR comment with final status
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.issue.number;
            const commitSha = context.payload.pull_request.head.sha.substring(0, 7);
            const commitUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/commit/${context.payload.pull_request.head.sha}`;
            const runUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const projectName = process.env.PROJECT_NAME;

            const buildResult = '${{ needs.build-and-push.result }}';
            const deployResult = '${{ needs.deploy.result }}';

            let body;

            if (buildResult === 'failure') {
              // Build failed
              body = `## ${projectName.charAt(0).toUpperCase() + projectName.slice(1).replace(/-/g, ' ')} – Preview

              | Name | Status | Preview | Updated (UTC) |
              |------|--------|---------|---------------|
              | **${projectName}-preview** | ❌ Build Failed ([${commitSha}](${commitUrl})) | [View Logs →](${runUrl}) | ${new Date().toISOString().replace('T', ' ').substring(0, 19)} |

              ---
              <sub>Docker build failed for commit ${commitSha} • [Check logs](${runUrl}) for details</sub>`;
            } else if (deployResult === 'failure') {
              // Deploy failed
              body = `## ${projectName.charAt(0).toUpperCase() + projectName.slice(1).replace(/-/g, ' ')} – Preview

              | Name | Status | Preview | Updated (UTC) |
              |------|--------|---------|---------------|
              | **${projectName}-preview** | ❌ Deployment Failed ([${commitSha}](${commitUrl})) | [View Logs →](${runUrl}) | ${new Date().toISOString().replace('T', ' ').substring(0, 19)} |

              ---
              <sub>Deployment failed for commit ${commitSha} • [Check logs](${runUrl}) for details</sub>`;
            } else if (deployResult === 'success') {
              // Success - get deployment URLs from deploy job outputs
              const customUrl = '${{ needs.deploy.outputs.custom_url }}';
              const serviceUrl = '${{ needs.deploy.outputs.service_url }}';
              
              let previewLinks = `[Custom Domain →](${customUrl})  • [Default URL →](${serviceUrl})`;
              let statusNote = '\n\n> ⏱️ **Note:** Custom domain may take 5-10 minutes to activate on first deployment. Use Default URL in the meantime.';
              
              body = `## ${projectName.charAt(0).toUpperCase() + projectName.slice(1).replace(/-/g, ' ')} – Preview

              | Name | Status | Preview | Updated (UTC) |
              |------|--------|---------|---------------|
              | **${projectName}-preview** | ✅ Ready ([${commitSha}](${commitUrl})) | ${previewLinks} | ${new Date().toISOString().replace('T', ' ').substring(0, 19)} |
              ${statusNote}

              ---
              <sub>Built with commit ${commitSha} • Preview environments are automatically destroyed when PRs are closed</sub>`;
            } else {
              // Unexpected state
              body = `## ${projectName.charAt(0).toUpperCase() + projectName.slice(1).replace(/-/g, ' ')} – Preview

              | Name | Status | Preview | Updated (UTC) |
              |------|--------|---------|---------------|
              | **${projectName}-preview** | ⚠️ Unknown Status ([${commitSha}](${commitUrl})) | [View Logs →](${runUrl}) | ${new Date().toISOString().replace('T', ' ').substring(0, 19)} |

              ---
              <sub>Unexpected workflow state for commit ${commitSha} • [Check logs](${runUrl}) for details</sub>`;
            }

            // Find and update existing comment
            const comments = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
            });

            const existingComment = comments.data.find(comment => 
              comment.user.type === 'Bot' && 
              comment.body.includes(`${projectName.charAt(0).toUpperCase() + projectName.slice(1).replace(/-/g, ' ')} – Preview`)
            );

            if (existingComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existingComment.id,
                body: body
              });
            }
      - name: Deployment summary
        if: needs.deploy.result == 'success'
        run: |
          echo "### Preview Environment Deployed! 🎉" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**PR:** #${{ github.event.pull_request.number }}" >> $GITHUB_STEP_SUMMARY
          echo "**Image Tag:** \`${{ needs.build-and-push.outputs.tag }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -n "${{ needs.deploy.outputs.custom_url }}" ]; then
            echo "**Custom Domain:** ${{ needs.deploy.outputs.custom_url }}" >> $GITHUB_STEP_SUMMARY
          fi
          echo "**Service URL:** ${{ needs.deploy.outputs.service_url }}" >> $GITHUB_STEP_SUMMARY

